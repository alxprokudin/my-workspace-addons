# 🚀 Настройка автоматической синхронизации с Google Apps Script

Это руководство поможет настроить автоматическую загрузку кода в Google Apps Script через GitHub Actions и clasp.

## 📋 Что такое clasp?

`clasp` (Command Line Apps Script Projects) - официальный инструмент Google для работы с Apps Script из командной строки. Он позволяет:
- Загружать код в Apps Script
- Скачивать код из Apps Script
- Управлять проектами через командную строку

## 🔧 Шаг 1: Установка clasp локально (для первоначальной настройки)

### Требования:
- Node.js (версия 14 или выше)
- npm (устанавливается вместе с Node.js)

### Установка:

```bash
npm install -g @google/clasp
```

Проверьте установку:
```bash
clasp --version
```

## 🔐 Шаг 2: Авторизация в Google

1. Включите Apps Script API:
   - Перейдите на https://script.google.com/home/usersettings
   - Включите "Google Apps Script API"

2. Авторизуйтесь через clasp:
```bash
clasp login
```

Это откроет браузер для авторизации. Войдите в свой Google аккаунт.

## 📝 Шаг 3: Создание проекта в Google Apps Script

### Вариант A: Создать новый проект

1. Откройте https://script.google.com
2. Нажмите "Новый проект"
3. Переименуйте проект (например, "Экспорт в Excel")
4. Скопируйте **Script ID** из URL:
   ```
   https://script.google.com/home/projects/YOUR_SCRIPT_ID/edit
   ```

### Вариант B: Использовать существующий проект

1. Откройте ваш проект в Apps Script
2. Скопируйте **Script ID** из URL

## 🔗 Шаг 4: Подключение локального проекта

1. Перейдите в папку проекта:
```bash
cd /Users/aleksandrprokudin/Documents/GW
```

2. Создайте файл `.clasp.json`:
```bash
cp .clasp.json.example .clasp.json
```

3. Откройте `.clasp.json` и вставьте ваш Script ID:
```json
{
  "scriptId": "ВАШ_SCRIPT_ID_ЗДЕСЬ",
  "rootDir": "."
}
```

4. Привяжите проект:
```bash
clasp clone YOUR_SCRIPT_ID
```

Или если проект уже существует:
```bash
clasp pull
```

## 📤 Шаг 5: Первая загрузка кода

1. Убедитесь, что файлы на месте:
   - `appsScript.js`
   - `dialog.html` (с реальными ключами!)
   - `appsscript.json`

2. Загрузите код:
```bash
clasp push
```

3. Проверьте в Apps Script, что файлы загрузились

## 🔑 Шаг 6: Настройка GitHub Actions

### 6.1 Получение токена для clasp

1. Создайте файл с токеном:
```bash
clasp login --creds creds.json
```

2. Скопируйте содержимое `creds.json` - это будет ваш `CLASP_TOKEN`

### 6.2 Добавление секретов в GitHub

1. Откройте ваш репозиторий на GitHub
2. Перейдите в **Settings** → **Secrets and variables** → **Actions**
3. Добавьте два секрета:

   **CLASP_TOKEN:**
   - Name: `CLASP_TOKEN`
   - Value: содержимое файла `creds.json`

   **CLASP_SCRIPT_ID:**
   - Name: `CLASP_SCRIPT_ID`
   - Value: ваш Script ID из `.clasp.json`

### 6.3 Альтернативный способ (через .clasprc.json)

Если у вас уже есть `.clasprc.json` после `clasp login`:

1. Найдите файл `~/.clasprc.json`
2. Скопируйте его содержимое
3. Добавьте как секрет `CLASP_TOKEN` в GitHub

## ✅ Шаг 7: Проверка работы

1. Внесите небольшое изменение в код (например, комментарий)
2. Закоммитьте и запушьте:
```bash
git add appsScript.js
git commit -m "Test deployment"
git push
```

3. Проверьте GitHub Actions:
   - Откройте вкладку **Actions** в репозитории
   - Должен запуститься workflow "Deploy to Google Apps Script"
   - Проверьте, что он выполнился успешно

4. Проверьте в Apps Script:
   - Откройте ваш проект
   - Убедитесь, что изменения применились

## 🔄 Как это работает

1. Вы делаете изменения в коде
2. Коммитите и пушите в ветку `excel-export-addon`
3. GitHub Actions автоматически:
   - Устанавливает clasp
   - Авторизуется с помощью токена
   - Загружает код в Apps Script
4. Код обновляется в Apps Script автоматически!

## ⚠️ Важные моменты

### Безопасность ключей

**ВАЖНО:** В `dialog.html` должны быть **реальные ключи** для работы в Apps Script, но они **НЕ должны** быть в репозитории!

**Решение:**
1. В репозитории храните `dialog.html.example` с плейсхолдерами
2. В Apps Script используйте `dialog.html` с реальными ключами
3. Добавьте `dialog.html` в `.gitignore` (если содержит ключи)
4. Или используйте GitHub Secrets для подстановки ключей в Actions

### Обновление dialog.html с ключами

Если нужно обновить `dialog.html` с реальными ключами через Actions:

1. Добавьте ключи как GitHub Secrets:
   - `DEVELOPER_KEY`
   - `CLOUD_PROJECT_NUMBER`

2. Обновите workflow для подстановки ключей (см. ниже)

## 🛠️ Расширенная настройка (опционально)

### Автоматическая подстановка ключей

Если хотите, чтобы ключи подставлялись автоматически, обновите workflow:

```yaml
- name: Prepare dialog.html with keys
  run: |
    sed -i "s/ВАШ_DEVELOPER_KEY_ЗДЕСЬ/${{ secrets.DEVELOPER_KEY }}/g" dialog.html
    sed -i "s/ВАШ_CLOUD_PROJECT_NUMBER_ЗДЕСЬ/${{ secrets.CLOUD_PROJECT_NUMBER }}/g" dialog.html
```

И добавьте секреты в GitHub:
- `DEVELOPER_KEY`
- `CLOUD_PROJECT_NUMBER`

## 📚 Полезные команды clasp

```bash
# Загрузить код в Apps Script
clasp push

# Скачать код из Apps Script
clasp pull

# Открыть проект в браузере
clasp open

# Посмотреть статус
clasp status

# Запустить функцию
clasp run functionName

# Посмотреть логи
clasp logs
```

## 🐛 Решение проблем

### Ошибка: "You do not have permission"
- Проверьте, что включен Apps Script API
- Проверьте авторизацию: `clasp login`

### Ошибка: "Script ID not found"
- Проверьте Script ID в `.clasp.json`
- Убедитесь, что проект существует

### Ошибка: "Authentication failed" в Actions
- Проверьте, что `CLASP_TOKEN` правильный
- Убедитесь, что токен не истек

### Код не обновляется
- Проверьте логи в GitHub Actions
- Убедитесь, что файлы в `.claspignore` правильные
- Попробуйте `clasp push --force` локально

## 📝 Чеклист настройки

- [ ] Node.js установлен
- [ ] clasp установлен (`npm install -g @google/clasp`)
- [ ] Apps Script API включен
- [ ] Авторизован через `clasp login`
- [ ] Проект создан в Apps Script
- [ ] `.clasp.json` создан с правильным Script ID
- [ ] Первая загрузка прошла успешно (`clasp push`)
- [ ] GitHub Secrets добавлены (CLASP_TOKEN, CLASP_SCRIPT_ID)
- [ ] GitHub Actions workflow работает
- [ ] Изменения автоматически загружаются

---

**Готово! Теперь ваш код будет автоматически синхронизироваться с Google Apps Script! 🎉**

