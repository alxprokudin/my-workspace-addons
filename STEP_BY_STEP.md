# 📝 Пошаговая настройка - выполняйте по порядку

## ✅ Шаг 1: Создание проекта в Google Apps Script

1. **Откройте:** https://script.google.com
2. **Нажмите:** "Новый проект" (или "+" вверху)
3. **Переименуйте проект:**
   - Нажмите на "Безымянный проект" вверху
   - Введите: **"Экспорт в Excel"**
4. **Скопируйте Script ID:**
   - Посмотрите на URL в адресной строке
   - Он выглядит так: `https://script.google.com/home/projects/ВАШ_SCRIPT_ID/edit`
   - Скопируйте часть `ВАШ_SCRIPT_ID` (длинная строка букв и цифр)

**Запишите Script ID здесь:** _________________________

---

## ✅ Шаг 2: Настройка .clasp.json

После того как получите Script ID, выполните в терминале:

```bash
cd /Users/aleksandrprokudin/Documents/GW
```

Затем создайте файл (замените YOUR_SCRIPT_ID на ваш реальный ID):

```bash
echo '{"scriptId":"YOUR_SCRIPT_ID","rootDir":"."}' > .clasp.json
```

**Или используйте готовый скрипт:**
```bash
./setup-clasp.sh
```

---

## ✅ Шаг 3: Подготовка dialog.html

**ВАЖНО:** Перед загрузкой нужно обновить `dialog.html` с реальными ключами!

1. **Откройте файл:** `dialog.html`
2. **Найдите строки:**
   ```javascript
   const DEVELOPER_KEY = "ВАШ_DEVELOPER_KEY_ЗДЕСЬ";
   const CLOUD_PROJECT_NUMBER = "ВАШ_CLOUD_PROJECT_NUMBER_ЗДЕСЬ";
   ```
3. **Замените на реальные значения:**
   - `DEVELOPER_KEY` = ваш Client ID из Google Cloud Console
   - `CLOUD_PROJECT_NUMBER` = ваш Project Number

**Если ключей еще нет:**
- См. `PUBLICATION_GUIDE.md` раздел "Настройка Google Cloud Console"
- Или выполните настройку позже, но без ключей код не будет работать

---

## ✅ Шаг 4: Первая загрузка кода

После настройки `.clasp.json` и обновления `dialog.html`:

```bash
clasp push
```

Если возникнут ошибки:
```bash
clasp push --force
```

**Проверьте в Apps Script:**
- Откройте ваш проект
- Убедитесь, что файлы появились:
  - `appsScript.js`
  - `dialog.html`
  - `appsscript.json`

---

## ✅ Шаг 5: Настройка GitHub Secrets

### 5.1 Получение токена

Выполните в терминале:
```bash
cat ~/.clasprc.json
```

Скопируйте **весь JSON объект** (начинается с `{` и заканчивается `}`)

### 5.2 Добавление секретов

1. **Откройте:** https://github.com/alxprokudin/my-workspace-addons/settings/secrets/actions

2. **Нажмите:** "New repository secret"

3. **Добавьте первый секрет:**
   - **Name:** `CLASP_TOKEN`
   - **Secret:** вставьте весь JSON из `~/.clasprc.json`
   - **Нажмите:** "Add secret"

4. **Добавьте второй секрет:**
   - **Name:** `CLASP_SCRIPT_ID`
   - **Secret:** ваш Script ID (из `.clasp.json`)
   - **Нажмите:** "Add secret"

---

## ✅ Шаг 6: Тестирование автоматизации

1. **Внесите небольшое изменение:**
   - Откройте `appsScript.js`
   - Добавьте комментарий, например: `// Test automatic deployment`

2. **Закоммитьте и запушьте:**
   ```bash
   git add appsScript.js
   git commit -m "Test automatic deployment"
   git push
   ```

3. **Проверьте GitHub Actions:**
   - Откройте: https://github.com/alxprokudin/my-workspace-addons/actions
   - Должен появиться workflow "Deploy to Google Apps Script"
   - Подождите 1-2 минуты
   - Проверьте, что статус ✅ зеленый

4. **Проверьте в Apps Script:**
   - Откройте ваш проект
   - Убедитесь, что комментарий появился в коде

---

## 🎉 Готово!

Теперь при каждом push код автоматически загрузится в Apps Script!

---

## ❓ Нужна помощь?

Если что-то не работает:
1. Проверьте логи в GitHub Actions
2. Проверьте, что секреты добавлены правильно
3. Убедитесь, что Script ID правильный
4. Проверьте, что `dialog.html` содержит реальные ключи

