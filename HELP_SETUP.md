# 🎯 Быстрая помощь по настройке

## Текущий статус:
✅ Node.js установлен
✅ clasp установлен  
✅ Авторизация в clasp выполнена
✅ Токен найден

## Что нужно сделать сейчас:

### 1️⃣ Создать проект в Google Apps Script (2 минуты)

**Откройте в браузере:**
```
https://script.google.com
```

**Действия:**
1. Нажмите **"Новый проект"** (или кнопку **"+"**)
2. Переименуйте проект: **"Экспорт в Excel"**
3. **Скопируйте Script ID из URL:**
   - URL выглядит так: `https://script.google.com/home/projects/1AbC2DeF3GhI4JkL5MnOpQrStUvWxYz/edit`
   - Script ID - это часть `1AbC2DeF3GhI4JkL5MnOpQrStUvWxYz`

**После получения Script ID, сообщите мне, и я помогу настроить дальше!**

---

### 2️⃣ Проверка dialog.html

Перед загрузкой нужно убедиться, что в `dialog.html` есть реальные ключи.

**Проверьте:**
- Откройте `dialog.html`
- Найдите строки с `DEVELOPER_KEY` и `CLOUD_PROJECT_NUMBER`
- Если там плейсхолдеры (`ВАШ_DEVELOPER_KEY_ЗДЕСЬ`), нужно заменить на реальные ключи

**Если ключей еще нет:**
- Можно загрузить код с плейсхолдерами
- Но дополнение не будет работать, пока не добавите реальные ключи
- Ключи можно добавить позже прямо в Apps Script

---

## 📋 После получения Script ID

Когда у вас будет Script ID, выполните:

```bash
cd /Users/aleksandrprokudin/Documents/GW
echo '{"scriptId":"ВАШ_SCRIPT_ID","rootDir":"."}' > .clasp.json
clasp push
```

Или используйте готовый скрипт:
```bash
./setup-clasp.sh
```

---

## 🔑 Настройка GitHub Secrets (после загрузки кода)

1. **Получите токен:**
   ```bash
   cat ~/.clasprc.json
   ```

2. **Добавьте в GitHub:**
   - Откройте: https://github.com/alxprokudin/my-workspace-addons/settings/secrets/actions
   - Добавьте секрет `CLASP_TOKEN` = содержимое `~/.clasprc.json`
   - Добавьте секрет `CLASP_SCRIPT_ID` = ваш Script ID

---

## ❓ Вопросы?

**Если Script ID уже есть:**
- Сообщите мне, и я помогу настроить `.clasp.json` и загрузить код

**Если нужны ключи для dialog.html:**
- См. `PUBLICATION_GUIDE.md` раздел "Настройка Google Cloud Console"
- Или можем настроить позже

**Если что-то не работает:**
- Покажите ошибку, и я помогу исправить

