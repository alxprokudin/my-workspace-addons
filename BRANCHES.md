# Структура репозитория

Этот репозиторий содержит несколько проектов в разных ветках.

## 🌿 Ветки

### `main` - Сайт проекта
Содержит только файлы статического сайта для GitHub Pages.

**Содержимое:**
- `docs/` - файлы сайта (HTML, CSS)
- `README.md` - описание сайта

**Использование:**
```bash
git checkout main
```

**Сайт:** https://alxprokudin.github.io/my-workspace-addons/

---

### `excel-export-addon` - Дополнение "Экспорт в Excel"
Содержит код Google Workspace Add-on для экспорта листов в Excel.

**Содержимое:**
- `appsScript.js` - основной код
- `dialog.html` - интерфейс выбора папки
- `appsscript.json` - манифест
- Документация по публикации

**Использование:**
```bash
git checkout excel-export-addon
```

---

## 🔄 Переключение между ветками

```bash
# Переключиться на ветку сайта
git checkout main

# Переключиться на ветку дополнения
git checkout excel-export-addon

# Посмотреть все ветки
git branch -a
```

## 📝 Работа с ветками

### Добавление нового дополнения

1. Создайте новую ветку от `main`:
   ```bash
   git checkout main
   git checkout -b new-addon-name
   ```

2. Добавьте файлы дополнения
3. Закоммитьте и запушьте:
   ```bash
   git add .
   git commit -m "Add new add-on"
   git push -u origin new-addon-name
   ```

### Обновление сайта

1. Переключитесь на `main`:
   ```bash
   git checkout main
   ```

2. Внесите изменения в `docs/`
3. Закоммитьте и запушьте:
   ```bash
   git add docs/
   git commit -m "Update website"
   git push
   ```

## 🎯 Рекомендации

- **main** - всегда содержит только файлы сайта
- Каждое дополнение - в отдельной ветке
- Название ветки должно соответствовать названию дополнения
- README в каждой ветке описывает содержимое этой ветки

---

**Текущая структура:**
- `main` → Сайт проекта
- `excel-export-addon` → Дополнение "Экспорт в Excel"

