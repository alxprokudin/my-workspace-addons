# 🚀 Инструкция по развертыванию на GitHub Pages

## Быстрая инструкция (5 минут)

### 1. Создайте репозиторий на GitHub

1. Перейдите на https://github.com/new
2. Название репозитория: `my-workspace-addons` (или любое другое)
3. Выберите **Public** (публичный)
4. **НЕ** добавляйте README, .gitignore или лицензию (файлы уже есть)
5. Нажмите **Create repository**

### 2. Загрузите файлы

**Вариант A: Через веб-интерфейс GitHub**

1. В созданном репозитории нажмите **"uploading an existing file"**
2. Перетащите все файлы из папки `docs/`:
   - `index.html`
   - `privacy-policy.html`
   - `styles.css`
   - `README.md`
3. Нажмите **Commit changes**

**Вариант B: Через Git (командная строка)**

```bash
# Перейдите в папку с файлами сайта
cd docs

# Инициализируйте git репозиторий
git init

# Добавьте все файлы
git add .

# Сделайте первый коммит
git commit -m "Initial commit: Add website files"

# Добавьте remote репозиторий (замените YOUR_USERNAME и YOUR_REPO)
git remote add origin https://github.com/YOUR_USERNAME/YOUR_REPO.git

# Загрузите файлы
git branch -M main
git push -u origin main
```

### 3. Включите GitHub Pages

1. В репозитории перейдите в **Settings** (Настройки)
2. В левом меню найдите **Pages**
3. В разделе **Source**:
   - Выберите **Deploy from a branch**
   - Branch: **main**
   - Folder: **/ (root)** (если файлы в корне) или **/docs** (если в папке docs)
4. Нажмите **Save**

### 4. Получите URL вашего сайта

GitHub автоматически создаст URL:
```
https://YOUR_USERNAME.github.io/YOUR_REPO_NAME/
```

Например: `https://johndoe.github.io/my-workspace-addons/`

**Важно**: Первая публикация может занять 1-5 минут.

### 5. Обновите контент

Откройте файлы и замените:

- `your-email@example.com` → ваш реальный email
- Обновите даты в `privacy-policy.html`
- Добавьте ссылку на Marketplace (когда дополнение будет опубликовано)

После изменений:
```bash
git add .
git commit -m "Update contact information"
git push
```

Изменения появятся на сайте через 1-2 минуты.

## ✅ Проверка

1. Откройте URL вашего сайта в браузере
2. Проверьте, что все страницы открываются:
   - Главная: `https://YOUR_USERNAME.github.io/YOUR_REPO/`
   - Политика: `https://YOUR_USERNAME.github.io/YOUR_REPO/privacy-policy.html`
3. Проверьте, что все ссылки работают
4. Проверьте на мобильном устройстве

## 🔗 Использование для OAuth Consent Screen

Скопируйте URL страницы политики конфиденциальности:
```
https://YOUR_USERNAME.github.io/YOUR_REPO/privacy-policy.html
```

Используйте этот URL в Google Cloud Console:
1. **APIs & Services** → **OAuth consent screen**
2. В поле **Privacy policy link** вставьте URL
3. Сохраните изменения

## 🎨 Кастомизация

### Изменение email

В файлах `index.html` и `privacy-policy.html` найдите:
```html
<a href="mailto:your-email@example.com">
```
Замените на ваш email.

### Добавление новых проектов

См. инструкции в `README.md` в разделе "Добавление нового проекта".

### Изменение цветов

Откройте `styles.css` и измените переменные в начале файла:
```css
:root {
    --primary-color: #1976d2;  /* Ваш цвет */
}
```

## 🐛 Решение проблем

### Сайт показывает 404

- Подождите 2-5 минут после первой публикации
- Проверьте, что GitHub Pages включен в Settings → Pages
- Убедитесь, что репозиторий публичный

### Стили не применяются

- Проверьте путь к CSS в HTML: `<link rel="stylesheet" href="styles.css">`
- Очистите кэш браузера (Ctrl+F5)

### Изменения не отображаются

- Подождите 1-2 минуты после push
- Очистите кэш браузера
- Проверьте, что файлы действительно загружены в репозиторий

## 📚 Дополнительные ресурсы

- [GitHub Pages Documentation](https://docs.github.com/en/pages)
- [Custom Domain для GitHub Pages](https://docs.github.com/en/pages/configuring-a-custom-domain)

---

**Готово! Ваш сайт опубликован! 🎉**

