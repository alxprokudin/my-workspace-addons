# 🔧 Решение проблемы с clasp authentication

## Проблема
Файл credentials создается (79 байт), но clasp не может его прочитать.

## Возможные причины

### 1. Секрет CLASP_TOKEN неполный или неправильный

**Проверка:**
- Локальный файл: ~632 байт
- В GitHub Actions: 79 байт
- **Вывод:** Секрет в GitHub обрезан или неправильно скопирован

**Решение:**
1. Откройте локальный файл:
   ```bash
   cat ~/.clasprc.json
   ```

2. Скопируйте **ВЕСЬ JSON** (от `{` до `}`)

3. В GitHub → Settings → Secrets → Actions:
   - Откройте `CLASP_TOKEN`
   - Удалите старый секрет
   - Создайте новый с **полным содержимым** файла

### 2. Формат файла неправильный

Убедитесь, что JSON валидный:
```bash
cat ~/.clasprc.json | jq .
```

Если ошибка - файл поврежден, нужно перегенерировать через `clasp login`

### 3. Альтернативное решение: Использовать Service Account

Если проблема сохраняется, можно использовать Service Account вместо OAuth токена.

---

## Быстрое решение

1. **Перегенерируйте токен:**
   ```bash
   clasp logout
   clasp login
   cat ~/.clasprc.json
   ```

2. **Обновите секрет в GitHub:**
   - Скопируйте весь JSON
   - Обновите `CLASP_TOKEN` в GitHub Secrets

3. **Повторите запуск workflow**

