# 🔑 Исправление секрета CLASP_TOKEN

## Проблема
- Локальный файл: **632 байта** ✅
- В GitHub Actions: **79 байт** ❌
- **Вывод:** Секрет в GitHub обрезан или неполный

## Решение

### Шаг 1: Получите полный токен

Выполните локально:
```bash
cat ~/.clasprc.json
```

Скопируйте **ВЕСЬ JSON объект** (от первой `{` до последней `}`)

### Шаг 2: Обновите секрет в GitHub

1. Откройте: https://github.com/alxprokudin/my-workspace-addons/settings/secrets/actions

2. Найдите секрет `CLASP_TOKEN`

3. Нажмите на карандаш (редактировать) или удалите и создайте заново

4. Вставьте **ПОЛНЫЙ JSON** из шага 1

5. Сохраните

### Шаг 3: Проверьте размер

После обновления, в следующем запуске workflow должно быть:
- File size: **632 bytes** (или около того)
- Не 79 байт!

### Шаг 4: Повторите запуск

После обновления секрета, workflow должен заработать.

---

## Важно

Убедитесь, что:
- ✅ JSON начинается с `{`
- ✅ JSON заканчивается на `}`
- ✅ Нет обрезанных строк
- ✅ Весь объект скопирован

---

## Альтернатива: Перегенерировать токен

Если проблема сохраняется:

```bash
clasp logout
clasp login
cat ~/.clasprc.json
```

Затем обновите секрет в GitHub с новым токеном.

