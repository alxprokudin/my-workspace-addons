# 🔐 Как работает OAuth для сторонних пользователей

## Как работает `ScriptApp.getOAuthToken()` для пользователей из магазина

### Процесс авторизации при установке

Когда пользователь устанавливает ваше дополнение из Google Workspace Marketplace:

1. **Установка дополнения:**
   - Пользователь нажимает "Установить" в Marketplace
   - Google проверяет OAuth Consent Screen настройки
   - Пользователю показывается экран согласия (OAuth Consent Screen)

2. **OAuth Consent Screen:**
   - Пользователь видит список запрошенных разрешений (scopes)
   - Пользователь видит название приложения, описание, иконку
   - Пользователь должен нажать "Разрешить" для предоставления доступа

3. **Авторизация:**
   - После нажатия "Разрешить" Google выдает OAuth токен
   - Токен сохраняется в контексте пользователя
   - Дополнение получает доступ к запрошенным scopes

### Как работает `ScriptApp.getOAuthToken()`

```javascript
const token = ScriptApp.getOAuthToken();
```

**Что происходит:**

1. **Первый вызов:**
   - Если пользователь еще не авторизован, Google автоматически показывает OAuth Consent Screen
   - Пользователь должен предоставить разрешения
   - После этого токен сохраняется

2. **Последующие вызовы:**
   - `ScriptApp.getOAuthToken()` возвращает сохраненный токен
   - Токен автоматически обновляется при истечении срока действия
   - Пользователю не нужно повторно авторизовываться

3. **Токен включает только запрошенные scopes:**
   - Токен содержит только те разрешения, которые указаны в `appsscript.json`
   - Если скрипт запрашивает scope, которого нет в манифесте, будет ошибка

### Текущие scopes в проекте

```json
"oauthScopes": [
  "https://www.googleapis.com/auth/spreadsheets.currentonly",
  "https://www.googleapis.com/auth/script.container.ui",
  "https://www.googleapis.com/auth/script.external_request"
]
```

**Что это означает для пользователя:**

1. **`spreadsheets.currentonly`:**
   - Доступ только к текущей открытой таблице
   - Пользователь видит: "Доступ к текущей таблице Google Sheets"

2. **`script.container.ui`:**
   - Доступ к UI (показ диалогов)
   - Пользователь видит: "Показ диалоговых окон"

3. **`script.external_request`:**
   - Доступ к внешним HTTP запросам
   - Пользователь видит: "Выполнение внешних HTTP запросов"

### Важные моменты

#### ✅ Что работает автоматически:

- **Автоматическая авторизация:** При первом использовании Google покажет экран согласия
- **Автоматическое обновление токена:** Токен обновляется автоматически при истечении
- **Безопасность:** Токен доступен только в контексте выполнения скрипта

#### ⚠️ Что нужно учесть:

1. **Срок действия токена:**
   - Токен действует ограниченное время (обычно 1 час)
   - `ScriptApp.getOAuthToken()` автоматически обновляет токен при необходимости
   - Не нужно кэшировать токен между вызовами

2. **Изменение scopes:**
   - Если вы добавите новые scopes в `appsscript.json`
   - Пользователю нужно будет повторно авторизоваться
   - Google покажет новый экран согласия с дополнительными разрешениями

3. **Ошибки авторизации:**
   - Если пользователь откажет в разрешениях, `ScriptApp.getOAuthToken()` вернет `null`
   - Нужно обрабатывать эту ситуацию в коде

### Пример обработки ошибок

```javascript
function getExcelBlobAsBase64() {
  try {
    // Получаем токен
    const token = ScriptApp.getOAuthToken();
    
    if (!token) {
      throw new Error('Не удалось получить токен авторизации. Пожалуйста, предоставьте необходимые разрешения.');
    }
    
    // Используем токен для запроса
    const response = UrlFetchApp.fetch(exportUrl, {
      headers: {
        Authorization: "Bearer " + token
      },
      muteHttpExceptions: true
    });
    
    // Проверяем ответ
    if (response.getResponseCode() === 401) {
      throw new Error('Токен истек или недействителен. Попробуйте снова.');
    }
    
    // ... остальной код
  } catch (error) {
    // Обработка ошибок
  }
}
```

### Что видит пользователь при установке

1. **В Marketplace:**
   - Название приложения
   - Описание
   - Иконка
   - Кнопка "Установить"

2. **При нажатии "Установить":**
   - OAuth Consent Screen с запрошенными разрешениями
   - Список scopes в понятном формате
   - Кнопки "Разрешить" и "Отмена"

3. **После установки:**
   - Дополнение доступно в меню Google Sheets
   - При первом использовании может быть дополнительная авторизация (если нужно)

### Проверка работы для сторонних пользователей

Для тестирования:

1. **Создайте тестового пользователя:**
   - В Google Cloud Console → OAuth Consent Screen
   - Добавьте тестовых пользователей в "Test users"

2. **Установите дополнение:**
   - Тестовый пользователь должен установить дополнение
   - Проверить, что OAuth Consent Screen показывается корректно

3. **Проверьте работу:**
   - Тестовый пользователь использует дополнение
   - Проверить, что `ScriptApp.getOAuthToken()` работает
   - Проверить, что экспорт работает корректно

### Дополнительные ресурсы

- [Официальная документация ScriptApp.getOAuthToken()](https://developers.google.com/apps-script/reference/script/script-app#getoauthtoken)
- [OAuth 2.0 для Google Workspace Add-ons](https://developers.google.com/workspace/add-ons/concepts/oauth)
- [OAuth Consent Screen настройка](https://support.google.com/cloud/answer/10311615)

