# 🔑 Объяснение OAuth Client IDs в Google Cloud Console

## ⚠️ Желтый восклицательный знак - что это?

Желтый восклицательный знак (⚠️) рядом с OAuth Client ID означает, что это **автоматически сгенерированный клиент**, который создан Google для работы вашего проекта.

**Это нормально и не является ошибкой!**

---

## 📋 Типы OAuth Client IDs

### 1. **Google Workspace Marketplace Integration Client**

- **Что это:** Автоматически созданный клиент для интеграции с Google Workspace Marketplace
- **Зачем нужен:** Для работы дополнения в Marketplace
- **Можно ли изменить:** Нет, это системный клиент
- **Можно ли удалить:** Нет, он требуется для работы дополнения

**Статус:** ✅ Нормально, оставьте как есть

### 2. **Apps Script**

- **Что это:** Автоматически созданный клиент для Google Apps Script
- **Зачем нужен:** Для работы вашего Apps Script проекта
- **Можно ли изменить:** Нет, это системный клиент
- **Можно ли удалить:** Нет, он требуется для работы проекта

**Статус:** ✅ Нормально, оставьте как есть

### 3. **Excel Export Add-on** (ваш собственный клиент)

- **Что это:** Клиент, который вы создали вручную
- **Зачем нужен:** Для работы Google Picker и других функций
- **Можно ли изменить:** Да, можно редактировать
- **Можно ли удалить:** Да, но не рекомендуется, если используется

**Статус:** ✅ Это ваш клиент, используйте его для Google Picker

---

## ❓ Часто задаваемые вопросы

### В: Нужно ли что-то делать с клиентами с желтым знаком?

**О:** Нет, ничего делать не нужно. Это автоматически созданные клиенты, которые требуются для работы дополнения. Просто оставьте их как есть.

### В: Можно ли удалить эти клиенты?

**О:** Нет, их нельзя удалить, так как они автоматически созданы Google и требуются для работы вашего проекта.

### В: Это ошибка?

**О:** Нет, это не ошибка. Желтый знак - это просто предупреждение о том, что клиент автоматически создан и не может быть изменен.

### В: Какой клиент использовать для Google Picker?

**О:** Используйте ваш собственный клиент "Excel Export Add-on" (тот, который вы создали вручную). Его Client ID нужно использовать в `dialog.html` как `DEVELOPER_KEY`.

---

## ✅ Что нужно сделать

**Ничего!** Просто оставьте все как есть:

- ✅ **Google Workspace Marketplace Integration Client** - оставьте (автоматический)
- ✅ **Apps Script** - оставьте (автоматический)
- ✅ **Excel Export Add-on** - используйте для Google Picker (ваш клиент)

---

## 🔍 Как проверить, что все правильно

1. ✅ У вас есть 3 OAuth Client IDs (2 автоматических + 1 ваш)
2. ✅ Автоматические клиенты имеют желтый знак (это нормально)
3. ✅ Ваш клиент "Excel Export Add-on" не имеет желтого знака
4. ✅ Client ID из "Excel Export Add-on" используется в `dialog.html`

---

## 📝 Резюме

**Желтый восклицательный знак = автоматически созданный клиент = нормально = ничего делать не нужно**

Просто продолжайте работу с дополнением. Эти клиенты созданы Google автоматически и требуются для работы вашего дополнения в Marketplace.

