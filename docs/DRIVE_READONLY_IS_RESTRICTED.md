# ⚠️ Важно: `drive.readonly` - это RESTRICTED scope!

## ❌ Проблема

Согласно официальной документации Google, **`drive.readonly` является RESTRICTED scope**, который требует:
- ✅ CASA верификацию (дорогая оценка безопасности)
- ✅ Ежегодную ресертификацию

Это **НЕ** то, что нам нужно!

## 🔍 Классификация scopes

### Restricted scopes (требуют CASA):
- `drive` - полный доступ к Drive
- `drive.readonly` - только чтение Drive ⚠️

### Sensitive scopes (требуют верификацию, но БЕЗ CASA):
- `drive.file` - доступ к файлам, созданным приложением или выбранным через Picker ✅

### Non-sensitive scopes (не требуют верификации):
- `spreadsheets.currentonly` - доступ к текущей таблице ✅

## ✅ Решение: Убрать `drive.readonly`

Нужно найти способ экспортировать отдельный лист **БЕЗ** `drive.readonly` scope.

### Варианты:

1. **Использовать временную копию таблицы с одним листом**
   - Создать копию таблицы с нужным листом
   - Экспортировать через SpreadsheetApp.getBlob()
   - Удалить временную копию

2. **Использовать Sheets API для получения данных и создать Excel вручную**
   - Сложнее, но работает с spreadsheets.currentonly

3. **Экспортировать всю таблицу** (не идеально, но работает)

## 🎯 Рекомендация

Попробовать вариант с временной копией таблицы - это позволит экспортировать отдельный лист без restricted scopes.

