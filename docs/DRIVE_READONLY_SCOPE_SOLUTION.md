# ✅ Решение: `drive.readonly` scope для экспорта отдельного листа

## 🎯 Проблема

Экспорт через URL `docs.google.com/spreadsheets/.../export` не работает с только `spreadsheets.currentonly` scope, так как требует доступа через Drive API.

## ✅ Решение: Добавить `drive.readonly` scope

### Что это дает:

- ✅ **Экспорт отдельного листа** (не всей таблицы)
- ✅ **Sensitive scope** (не restricted)
- ✅ **НЕ требует CASA** - только стандартная верификация Google
- ✅ **Работает с экспортом через URL**

### Scopes:

```json
"oauthScopes": [
  "https://www.googleapis.com/auth/spreadsheets.currentonly",
  "https://www.googleapis.com/auth/drive.file",        // Для сохранения в папку
  "https://www.googleapis.com/auth/drive.readonly",    // Для экспорта через URL
  "https://www.googleapis.com/auth/script.external_request",
  "https://www.googleapis.com/auth/script.container.ui"
]
```

## 📋 Как это работает

1. **Экспорт:** `drive.readonly` дает доступ к чтению таблицы через Drive API, что позволяет экспортировать через URL
2. **Сохранение:** `drive.file` + Google Picker позволяет сохранять файл в выбранную папку

## ⚠️ Верификация

Оба scope (`drive.file` и `drive.readonly`) являются **sensitive scopes**:
- ✅ Требуют стандартную верификацию Google
- ✅ НЕ требуют CASA (дорогую оценку безопасности)
- ✅ Процесс верификации бесплатный

## 🎯 Преимущества

- ✅ Экспорт отдельного листа (не всей таблицы)
- ✅ Сохранение в выбранную папку через Google Picker
- ✅ Без CASA верификации
- ✅ Только стандартная верификация Google

## 📝 Что нужно для верификации

1. Заполнить обоснования для scopes
2. Загрузить демо-видео
3. Верифицировать домен
4. Ответить на вопросы Google

**Без CASA!** ✅

