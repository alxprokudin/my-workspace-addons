# 🔧 Исправление ошибки доступа к папке

## ❌ Ошибка

```
Ошибка: Ошибка при экспорте: Error: Не удалось получить доступ к папке. Проверьте права доступа.
```

## 🔍 Причина

Scope `drive.file` дает доступ только к файлам и папкам, **созданным приложением**. Когда пользователь выбирает существующую папку через Google Picker, эта папка может быть недоступна, если она не была создана приложением.

---

## ✅ Решение 1: Использовать более широкий scope (рекомендуется)

### Шаг 1: Добавьте scope `drive` в appsscript.json

1. Откройте [Google Apps Script](https://script.google.com)
2. Откройте проект "Экспорт в Excel"
3. Откройте файл `appsscript.json`
4. **Замените** `drive.file` на `drive`:

```json
{
  "timeZone": "Europe/Moscow",
  "dependencies": {
    "enabledAdvancedServices": []
  },
  "exceptionLogging": "STACKDRIVER",
  "runtimeVersion": "V8",
  "oauthScopes": [
    "https://www.googleapis.com/auth/spreadsheets.currentonly",
    "https://www.googleapis.com/auth/drive",
    "https://www.googleapis.com/auth/script.external_request",
    "https://www.googleapis.com/auth/script.container.ui"
  ],
  "sheets": {
    "homepageTrigger": {
      "runFunction": "onOpen"
    }
  }
}
```

**Изменение:** `drive.file` → `drive`

5. **Сохраните файл**

---

### Шаг 2: Добавьте scope `drive` в OAuth Consent Screen

1. Откройте [Google Cloud Console](https://console.cloud.google.com)
2. Выберите ваш проект
3. Перейдите в **"APIs & Services"** → **"OAuth consent screen"**
4. Прокрутите вниз до раздела **"Scopes"**
5. Нажмите **"+ ADD OR REMOVE SCOPES"**
6. **Удалите** `https://www.googleapis.com/auth/drive.file`
7. **Добавьте** `https://www.googleapis.com/auth/drive`
8. Нажмите **"UPDATE"**
9. Нажмите **"SAVE AND CONTINUE"**

**Важно:** Scope `drive` дает доступ ко всем файлам и папкам пользователя в Google Drive, что необходимо для сохранения в выбранную папку.

---

### Шаг 3: Создайте новое развертывание

1. В Apps Script перейдите в **"Развернуть"** (🚀) → **"Управление развертыванием"**
2. Создайте новое развертывание типа "Add-on"
3. Это обновит манифест в развертывании

---

### Шаг 4: Попросите тестового пользователя переустановить дополнение

**Критически важно:** Тестовый пользователь должен переустановить дополнение, чтобы получить новые разрешения!

1. Попросите тестового пользователя:
   - **Удалить дополнение полностью**
   - **Установить дополнение заново**
   - **Разрешить новое разрешение** `drive` (доступ ко всем файлам в Google Drive)
   - **Протестировать снова**

---

## ✅ Решение 2: Альтернативный подход (если не хотите использовать `drive`)

Если вы не хотите запрашивать полный доступ к Drive, можно попробовать другой подход:

### Вариант A: Создать файл в корне Drive, затем переместить

Это сложнее и требует дополнительных разрешений.

### Вариант B: Использовать `drive.file` с созданием папки

Создавать папку через приложение, но это не подходит, если пользователь хочет выбрать существующую папку.

**Рекомендация:** Используйте Решение 1 (scope `drive`). Это стандартный подход для дополнений, которые работают с файлами пользователя.

---

## 📋 Разница между scopes

### `drive.file` (ограниченный доступ)
- ✅ Доступ только к файлам/папкам, созданным приложением
- ❌ Не может сохранить файл в существующую папку пользователя
- ✅ Меньше разрешений (более безопасно)

### `drive` (полный доступ)
- ✅ Доступ ко всем файлам и папкам пользователя в Google Drive
- ✅ Может сохранить файл в любую выбранную папку
- ⚠️ Больше разрешений (менее безопасно, но необходимо для функциональности)

**Для вашего дополнения:** Нужен scope `drive`, так как пользователь выбирает существующую папку.

---

## ✅ Чеклист исправления

- [ ] Scope `drive.file` заменен на `drive` в `appsscript.json`
- [ ] Scope `drive` добавлен в OAuth Consent Screen
- [ ] Scope `drive.file` удален из OAuth Consent Screen
- [ ] Создано новое развертывание
- [ ] Тестовый пользователь переустановил дополнение
- [ ] Тестовый пользователь разрешил новое разрешение `drive`
- [ ] Ошибка доступа к папке исчезла
- [ ] Файл успешно сохраняется в выбранную папку

---

## 🔒 Безопасность scope `drive`

**Важно:** Scope `drive` запрашивает доступ ко всем файлам пользователя в Google Drive. Это необходимо для функциональности дополнения, но:

1. **Объясните пользователям** в описании дополнения, почему нужен этот доступ
2. **Используйте только для сохранения** файлов в выбранную папку
3. **Не читайте** другие файлы пользователя без необходимости

---

## ❓ Часто задаваемые вопросы

### В: Можно ли использовать `drive.file` вместо `drive`?

**О:** Технически можно, но тогда пользователь сможет сохранять файлы только в папки, созданные приложением. Для сохранения в выбранную пользователем папку нужен scope `drive`.

### В: Безопасно ли использовать scope `drive`?

**О:** Да, если вы используете его только для сохранения файлов в выбранную папку. Google проверяет дополнения на соответствие заявленным функциям.

### В: Что делать, если пользователь не хочет давать полный доступ к Drive?

**О:** Это ограничение функциональности. Можно предложить альтернативу - сохранять файлы в специальную папку, созданную приложением, но это менее удобно для пользователя.

---

## 🔗 Полезные ссылки

- [Google Drive API Scopes](https://developers.google.com/drive/api/guides/about-auth)
- [OAuth Consent Screen](https://console.cloud.google.com/apis/credentials/consent)
- [Google Apps Script - DriveApp](https://developers.google.com/apps-script/reference/drive/drive-app)

